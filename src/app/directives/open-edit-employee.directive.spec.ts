import { OpenEditEmployeeDirective } from './open-edit-employee.directive';

describe('OpenEditEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenEditEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
